# Python packages used by rover_high_level (same list as pyproject.toml).
# Install everything at once from the project root with: python3 -m pip install -e .
huggingface_hub
numpy
ollama
opencv-contrib-python
pillow
pynput
requests
stable-baselines3
transformers
ultralytics

# Note: ultralytics pulls in torch. On a machine with no NVIDIA GPU, install the
# much smaller CPU-only torch first: python3 -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
